<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <property name="LOG_ROOT" value="./" />
    <property name="LOG_FILE_NAME" value="srvc-apiadapter" />

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{"yyyy-MM-dd HH:mm:ss,SSS"} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_ROOT}/${LOG_FILE_NAME}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_ROOT}/${LOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <!-- each archived file's size will be max 10MB -->
            <maxFileSize>20MB</maxFileSize>
            <!-- 5 days to keep -->
            <maxHistory>5</maxHistory>
            <!-- total size of all archive files, if total size > xxxGB, it will delete old archived file -->
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>%d{"yyyy-MM-dd HH:mm:ss,SSS"} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="org.apache.zookeeper.ZooKeeper" level="WARN" />
    <logger name="org.apache.zookeeper.ClientCnxn" level="WARN" />
    <logger name="org.apache.curator" level="WARN" />

</configuration>
